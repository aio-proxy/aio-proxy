# SDD ledger — plan: docs/superpowers/plans/2026-09-08-config-sync-integration.md

## Setup

- Workspace: `/Users/bytedance/.codex/worktrees/e136/aio-proxy/.superpowers/sdd/2026-09-08-config-sync-integration/`
- Worktree: linked detached worktree; no main/master branch is touched.
- Spec: `docs/superpowers/specs/2026-09-08-config-sync-design.md`
- Plan: `docs/superpowers/plans/2026-09-08-config-sync-integration.md`
- All implementers and reviewers are required to use `gpt-5.6-luna`.

## Task status

- Task 1: review found 7 Critical/Important lifecycle and activation gaps; fix round 1/5 resumed on `/root/integration_task1`.
- Task 2: implementation commit `4205b27f2` from base `b3529ec9`; report `.superpowers/sdd/2026-09-08-config-sync-integration/task-2-report.md`. Independent review package `.superpowers/sdd/2026-09-08-config-sync-integration/review-b3529ec9..4205b27f.diff` dispatched to `/root/integration_task2_review`; verdict pending.
- Task 2 review: FAIL with 8 Critical and 7 Important findings plus quality gaps. Fix round 1/5 resumed on `/root/integration_task2`; scoped re-review will use the fix range after the report/commit.
- Task 2 fix round 1 commit `21f24c30` added metadata-driven redaction, CAS/version fences, history-aware restore, overrides, dependency-aware purge, conflict/rejoin persistence, backend validation, and expanded regression coverage. Scoped re-review package `.superpowers/sdd/2026-09-08-config-sync-integration/review-4205b27f..21f24c30.diff` dispatched to `/root/integration_task2_rereview_fix1`; verdict pending.
- Scoped re-review: NOT READY. Two Critical TOCTOU/one-snapshot fence gaps remain, plus Important forbidden metadata paths, plugin purge identity/dependency closure, and same-ID persistence/reference mapping. Fix round 2/5 resumed on `/root/integration_task2`; concurrent OAuth source-account finding is carried by the OAuth Task 5 loop.
- Task 2 fix round 2 commit `d9f4a7dd` adds conditional core CAS for publish/delete/restore/purge, frozen snapshot mutation, metadata restrictions, transitive/local-only purge handling, atomic same-ID persistence, and regression coverage. Scoped re-review package `.superpowers/sdd/2026-09-08-config-sync-integration/review-21f24c30..d9f4a7dd.diff` dispatched to `/root/integration_task2_rereview_fix2`; verdict pending.
- The first round-2 reviewer attempt returned transient 503 before producing a verdict; a fresh `gpt-5.6-luna` retry `/root/integration_task2_rereview_fix2_retry` was dispatched against the same package.
- Round-2 scoped verdict: NOT READY. Remote CAS/frozen snapshot passed, but a Critical local snapshot/fence race remains; Important equivalent forbidden paths and account-store/same-ID validation remain, with a purge local-only dependency edge. Fix round 3/5 resumed on `/root/integration_task2`.
- Tasks 3–5: pending the public control-plane contracts.

## Task 1 review findings

- Critical: remote deletion/purge is a no-op in the server local port, so the runtime Provider remains active.
- Critical: dependency-aware activation is not wired into remote reconciliation; missing plugins/env, incompatible versions, invalid credentials, OAuth evidence and identity collisions bypass gates.
- Critical: OAuth login/import and external file reload do not prepare/confirm sync commits; only config-store mutations are captured.
- Important: startup recovery runs after snapshot/runtime construction instead of before it.
- Important: remote application bypasses the FIFO/config ownership fence.
- Important: binding/session generation is not rechecked after awaited connection, stale sessions are not reliably disposed, and callbacks use the wrong binding.
- Important: shutdown/startup error paths can skip database close or async session disposal.
- Quality: lifecycle fixtures do not observe real async disposal/order and required capture/deletion/FIFO/recovery coverage is missing.

The fix must preserve the existing OAuth ownership metadata and keep public control-plane operations in Task 2; it must not claim live CloudKit/OAuth evidence.

## Task 1 fix round 1 re-review

- Excluded-local preservation, dependency/version/credential pending state, startup ordering, connect-time binding fencing and remote FIFO serialization are addressed.
- Important gaps remain for legacy close ordering, generation checks inside the FIFO mutation, full activation identity/OAuth evidence, account-removal confirmation after drain, and real disposal-before-DB-close fixture coverage.
- Fix round 2/5 resumed on `/root/integration_task1`.
- Fix round 2: `54322f09c` fixes legacy close ordering, adds in-fence generation checks, validates account identity/credentialSync evidence, re-confirms account-removal commits after drain, and adds real disposal-before-DB-close coverage. Awaiting scoped re-review.
- Scoped re-review after `54322f09c`: legacy close, prerequisite generation fence, post-drain account confirmation are addressed. Important gaps remain for a post-`applyCandidate` generation fence and exact incoming OAuth account identity/evidence validation; startup-failure disposal coverage is a Minor test gap. Fix round 3/5 resumed on `/root/integration_task1`.
- Fix round 3: `892ee9f0d` adds the post-`applyCandidate` generation fence, validates incoming OAuth identity/version/format/phase/evidence, and adds a bound startup-unwind fixture. Scoped re-review: all prior findings addressed; no new Critical/Important/Minor regressions; 137 targeted tests passed.
- Task 1: complete (commits `3600adf39..892ee9f0d`, review clean).
- Round-3 scoped review: local snapshot/fence, frozen cloud dependency purge and same-ID mapping/uncertain mutation handling addressed. Two findings remain: optional persistProviderIdentity hook now fails valid fallback paths, and accountProviderId remains an overrideable provider-reference metadata path. Fix round 4/5 dispatched to fresh implementer.
- Fix round 4 commit `c5f4dc7e` restores atomic repository fallback for same-ID persistence, supports single-row fallback, rejects multi-row mappings without atomic persistence, and rejects accountProviderId provider-reference overrides. Focused tests/checks pass; scoped re-review dispatched.
- Fix round 4 scoped re-review: all functional findings addressed and no regression, but `packages/server/src/sync-control-plane/preview.ts` grew to 527 lines. Fix round 5/5 dispatched to split the oversized implementation without changing behavior.
- Fix round 5 commit `6c5f0c04` extracts override projection and preview errors; `preview.ts` is now 456 lines. Focused tests/checks pass; final scoped re-review dispatched at the round cap.
- Task 2 final scoped re-review: PASS after fix round 5; all functional findings addressed, extracted private collaborators preserve exports/behavior, and `preview.ts` is 456 lines. Server sync 17 tests, types 1 test, lint/format/type/diff checks passed.
- Task 2: complete (commits `4205b27f..6c5f0c04`, review clean after fix rounds 1–5).
- Task 3 implementation commit `ab8737680` adds authenticated typed sync routes, secret-safe stable errors, sync.changed events, and actual auth/loopback/Origin/Host coverage. Review package `.superpowers/sdd/2026-09-08-config-sync-integration/review-6c5f0c04..ab8737680.diff` dispatched; verdict pending.
- Task 3 review: parent auth/mount/redaction and route surface pass, but Important fixes required: TypeError from backend/network is misclassified as 400 invalid-request instead of backend-unavailable/503; route tests omit dependency-in-use and operation-pending mutation codes. Minor coverage gaps in configured secret DTOs and schema parsing are recorded for review triage.
- Fix round 1/5 resumed on `/root/integration_task3`.
- Task 3 fix round 1 commit `969fb6562` maps generic TypeError/backend failures to redacted backend-unavailable/503 and adds dependency-in-use/operation-pending route tests. Focused tests/checks pass; scoped re-review dispatched.
- Task 3 scoped re-review: PASS; TypeError/backend failures now map to backend-unavailable/503, conflict codes have route tests, and no new Critical/Important/Minor regressions. Focused server/auth 24 tests and type 39 tests pass.
- Task 3 minor deferred: the actual createServer fixture uses a disconnected empty sync state, so configured secret DTO redaction and runtime output-schema parsing are not independently exercised; stable route redaction/auth behavior is covered and the control-plane tests cover secret-safe DTO construction. Cost if wrong: a future DTO wiring regression could evade route-level fixture coverage.
- Task 3: complete (commits `ab8737680..969fb6562`, review clean after fix round 1).
- Task 4 implementation commit `3465cc0bd` adds typed Dashboard sync services/hooks, Settings/Provider controls, previews/history/overrides, five-locale copy and focused tests. Review package `.superpowers/sdd/2026-09-08-config-sync-integration/review-969fb6562..3465cc0bd.diff` dispatched; verdict pending.
- Task 4 review: FAIL. Critical: Provider purge uses the first Provider objectId while server matches provider logicalKey, so purge previews no rows. Critical: override editor never emits an overrides preview and parent omits onPreviewOverrides. Important: detach can silently no-op/hide pending state; secret dependency disclosure ignores secretChange; preview mutation does not invalidate status; form/Zod validation is incomplete; failures are mostly invisible; history table omits required TanStack Table capabilities; required behavior tests are missing. Minor hardcoded symbols/time semantics/changeset wording deferred until fix review.
- Fix round 1/5 resumed on `/root/integration_task4`.
- Task 4 fix round 1 commit `33c84f162` wires purge/override/detach flows, error displays, mutation invalidation, Zod/TanStack boundaries, history table capabilities and regression tests. Dashboard 982 tests pass; scoped re-review dispatched.
- Task 4 fix round 1 re-review: purge target, detach/pending disclosure, secretChange, invalidation, forms/Zod, errors and history controls addressed. Open Important: removing an override pin permanently disables Apply with no fresh-preview action; Settings wires override preview into purge, converting purge into overrides and breaking requested purge. Behavior-test coverage for removal/focus/pending/history is incomplete. Fix round 2/5 resumed on `/root/integration_task4`.
- Task 4 fix round 2 commit `5d55ff352` keeps purge previews as purge, requests exact replacement override previews on add/remove, persists paths, and disables apply during invalid/pending replacement. Focused 11 tests and full Dashboard 985 tests pass; scoped re-review dispatched.
- Task 4 fix round 2 re-review: purge-only and successful add/remove replacement flows pass, but Important gaps remain: failed override replacement has no retry for the same path set, and closing/reopening a dialog retains `overridesRef` pins because preview=null does not clear it. Fix round 3/5 resumed on `/root/integration_task4`.
- Task 4 fix round 3 commit `d8e73d0e` preserves override paths for localized retry after refresh failure and clears them on close/reopen, with regression tests and translations. Dashboard 987 tests pass; scoped re-review dispatched.
- Task 4 final scoped re-review: PASS after fix round 3; failed override refresh retry and close/reopen cleanup are addressed, purge-only and pending/wrong-kind guards remain correct, and no new Critical/Important/Minor regressions were found. Dashboard 987 tests pass.
- Task 4: complete (commits `3465cc0bd..d8e73d0e`, review clean after fix rounds 1–3).
- Task 5 implementation commit `55d4d8be` adds the complete CLI sync command tree, existing-service auth/password flow, redacted preview/apply output, detach/overrides/options handling, docs and five-locale copy. Focused CLI 30 tests and i18n 11 tests pass; full CLI has 13 documented pre-existing macOS path failures. Review package `.superpowers/sdd/2026-09-08-config-sync-integration/review-d8e73d0e..55d4d8be.diff` dispatched; verdict pending.
- Task 5 review: FAIL. Critical: production ServerState never constructs/exposes SyncControlPlane, so routes/CLI always use unavailableSync. Important: detach submits an initial OAuth session ID without polling/completing authorization; wildcard binds use raw 0.0.0.0/:: instead of loopback canonicalization; real-service test has no configured binding/engine and does not prove CLI mutations/auth. Minor: human preview omits local/cloud/dependency detail and dashboard_unavailable/stdin edge cases. Fix round 1/5 resumed on `/root/integration_task5`.
- Task 5 fix round 1 commit `8b1d8468` wires a real SyncControlPlane into ServerState, completes OAuth detach polling, canonicalizes wildcard endpoints, hardens redacted output/stdin and adds configured-service/auth/one-engine fixtures. Focused suite 82 tests passes; scoped re-review dispatched.
- Task 5 fix round 1 re-review: production wiring, detach polling, wildcard canonicalization and configured-service fixture addressed. Open Important: connectHost() now rejects configured non-loopback hosts despite supported password-protected remote Dashboard binds; sync connect remains unconditional backend-unavailable so backend switching promised by CLI/docs cannot complete. Minor: server.ts grew to 512 lines. Fix round 2/5 resumed on `/root/integration_task5`.
- Task 5 fix round 2 commit `8bcb774f` supports authenticated remote hosts, implements reviewed backend switching with one server-owned engine, and extracts server options (`server.ts` 487 lines). Focused 102 tests and checks pass; scoped re-review dispatched.
- Task 5 fix round 2 re-review: remote host handling and CLI boundaries pass. Open Important: connect discards safeParse().data/defaulted options, and replaceBackend closes/persists old state before new lifecycle activation without rollback on failure. Minor: one-engine test uses no binding/config and only exercises unavailable fallback. Fix round 3/5 resumed on `/root/integration_task5`.
- Task 5 fix round 3 commit `2b05d995` uses validated backend defaults, makes backend switching transactional with candidate rollback/lifecycle isolation, and adds disposal/engine/status/binding/rollback coverage. 102 focused tests/checks pass; scoped re-review dispatched.
- Task 5 fix round 3 re-review: options/defaults, ordinary rollback and configured-service coverage pass. Open Important: preconnected candidate session ownership is cleared before early lifecycle setup/recovery can fail, leaking sessions; concurrent backend applies are not serialized and can orphan candidate engines/watchers. Fix round 4/5 dispatched to fresh implementer.
- Task 5 fix round 4 commit `3bee4b0` fences preconnected session ownership across early lifecycle failures, serializes backend connects FIFO, and adds repeated-failure/concurrent-replacement regressions. Focused 26 tests/checks pass; scoped re-review dispatched.
- Task 5 final scoped re-review: PASS after fix round 4; candidate session disposal and concurrent connect serialization are addressed, with no new Critical/Important/Minor regressions. Focused sync/CLI 34 tests pass.
- Task 5: complete (commits `55d4d8be..3bee4b0`, review clean after fix rounds 1–4).
- Task 6 commits `d8729b6f1` and `b788b62a4` add the deterministic two-server acceptance scenario, signed CloudKit release prerequisites, release-script artifact/SDK checks, consolidated changeset/docs. Acceptance and deterministic gates pass; real signing/CloudKit/OAuth gates remain blocked and preflight retains baseline errors. Review package `.superpowers/sdd/2026-09-08-config-sync-integration/review-3bee4b04..b788b62a.diff` dispatched; verdict pending.
- Task 6 review: FAIL. Critical: release workflow/script mismatch makes non-release dry-run require CLOUDKIT_SIGNED_ARCHIVE; acceptance matrix is only one six-assertion test and misses required conflict/history/identity/unknown/OAuth/plugin cases. Important: fixture pre-seeds plugin secret on both devices; release accepts unsigned archive; CloudKit SDK is dependency not required peer; signing password is passed in argv; artifact.test does not inspect packed native artifact. Fix round 1/5 resumed on `/root/integration_task6`.
- Task 6 fix round 1 commit `09868ee6` aligns release dry-run/CloudKit gating, validates signed artifact metadata and peer dependency, removes signing password argv use, strengthens artifact tests, and expands deterministic acceptance/restore coverage. Scoped re-review dispatched.
- Task 6 fix round 1 re-review: dry-run, secret seeding, deterministic matrix, signed production validation, peer/lockstep and metadata mostly addressed. Open Critical: release workflow deletes CLOUDKIT_SIGNED_MANIFEST before Changesets publish. Important: signing password still relies on GUI despite stdin; synthetic OAuth does not exercise login/token families/canDetach; artifact test does not inspect npm tarball; stale bun.lock/release report evidence remain. Fix round 2/5 resumed on `/root/integration_task6`.
- Task 6 fix round 2 commit `e0b5491f7` preserves signed manifest through publish/cleanup, adds headless-safe signing workflow checks, fake OAuth/token-family/canDetach acceptance coverage, tarball artifact checks, peer/lockfile updates, and corrected report fences. Focused 30 sync, 9 artifact, 16 CloudKit and 2 workflow tests pass; scoped re-review dispatched.
- Task 6 fix round 2 re-review: release ordering/password, public OAuth acceptance, tarball/artifact checks, peer/lockfile and blocked-gate docs addressed. Open Important: new TS2322 in acceptance-oauth.ts; release dry-run mutates tracked bun.lock via bun update/packing. Minor report omits both diagnostics. Fix round 3/5 resumed on `/root/integration_task6`.
- Task 6 fix round 3 commit `20e774d4` fixes acceptance OAuth descriptor typing, makes release dry-run restore lock/package files in finally, adds no-drift/no-npm regression coverage, and updates diagnostics. Sync 30, artifact 9, CloudKit 16, workflow 3 tests and frozen install/check pass; preflight retains documented baseline diagnostics. Scoped re-review dispatched.
- Task 6 final scoped re-review: PASS after fix round 3. Acceptance OAuth typing and release dry-run zero-drift are addressed; no new Critical/Important/Minor regressions. Sync 30, artifact 9, workflow 3 tests pass; only documented baseline preflight diagnostics remain.
- Task 6: complete (commits `d8729b6f1..20e774d4`, review clean after fix rounds 1–3).
- Whole-branch review from `ef5f8fb5..20e774d4` found one Critical and three Important cross-task defects: remote-origin imports can echo through the default watcher; plugin secrets are finalized after runtime validation and tombstones do not delete local secret rows; control-plane baseline stores remote.version while engine expects revision operation IDs. Minor: core sync repository is 517 lines. One final fix wave is required before the single scoped re-review.
- Whole-branch final fix commit `090de0b9f` addresses watcher no-echo fencing, plugin secret ordering/rollback/tombstone cleanup, revision-based baselines, and repository file splitting. Core 2,008 and server sync/state 68 tests pass; final scoped re-review dispatched.
- Final scoped re-review after `090de0b9f`: watcher suppression, ordering, tombstone path, baseline identity and repository split were partially addressed, but three Important findings remain open.
- Ruling: tombstone `deletePluginSecret` CAS failure is load-bearing and remains open; sync must not confirm/advance the tombstone baseline until deletion is confirmed. Cost if wrong: a stale plugin secret can survive purge and be re-shared.
- Ruling: plugin-secret mutation is not represented in durable commit recovery and remains open; a crash can confirm a remote commit without reconciling secret-only state. Cost if wrong: local secret/config state can diverge after restart and a credential may be lost or resurrected.
- Ruling: remote apply's unlocked read and uncertain lock-release fence clearing remains open; an external edit can be overwritten or an uncertain remote commit can echo. Cost if wrong: user edits/data can be lost and shared state can receive a duplicate publication.
- Final branch assessment: not ready to merge; no second final fix wave is dispatched under the subagent-driven-development final-review rule. Live CloudKit/OAuth gates remain blocked separately.
- Final fix wave: tombstone secret deletion now treats a failed CAS as `secret-conflict`, preserves the secret and baseline, and leaves the remote journal pending; plugin-secret mutations are persisted in commit intents and checked during restart recovery; remote config apply validates the expected digest under the AtomicConfigFile lock and retains the watcher fence through lock-release uncertainty. Regression coverage was added for CAS conflict, secret-only crash/reopen unknown state, stale external edits, and watcher lock-release retry.
- Final fix validation: the focused core/config/local-commit/repository/engine suite passed 62 tests, and the focused server sync-control-plane/server-state/acceptance suite passed 20 tests, including watcher lock-release and stale-digest integration regressions. `bun run check`, touched-file type-aware oxlint, core build, and `git diff --check` passed; repository-wide type diagnostics remain the documented baseline outside touched files.
- Residual scoped finding: a second `applyRemote` for a prepared `remote:${objectId}:${operationId}` could reprepare from live state, throw `Conflicting sync commit`, discard the durable journal, or overwrite an unknown plugin secret.
- Residual fix ruling: prepared remote intents are authoritative and idempotent. Retries compare raw config and plugin secrets against stored before/after values, return stable `invalid-config`/`secret-conflict` pending results for unknown external state, retry before-state sides with the stored target and current CAS revision, skip after-state sides, and confirm only when both stored after-states match. Tombstones continue through the same secret CAS path.
- Residual fix evidence: same-operation secret-change and config-change retry regressions pass; the focused server/core suite passes 47 tests, `bun run check` passes with documented pre-existing warnings only, and `git diff --check` passes.
- Final residual scoped re-review: PASS. Prepared remote retries reuse the durable before/after intent, preserve unknown external configuration and plugin secrets, and avoid conflicting re-prepares or journal discard. Focused local-port tests passed 7/7; no new Critical/Important/Minor regressions.
- Integration plan: complete through final residual review (commits `090de0b9f..472d58ef`). Live CloudKit signing, installed-path/launchd, two-Mac, and provider OAuth gates remain blocked and are not release evidence.

## Post-plan verification repair

- The required repository-wide preflight exposed a false positive in `packages/core/__tests__/build-entry.smoke.ts`: legitimate `sync/repository/` internal imports were still matched as stale moved-plugin imports.
- Task 7 brief: `task-7-brief.md`; base commit: `432e801ff245260abd3d46775be0b57be22c0aae`.
- Task 7 implementation commit `4bab7e556` narrows the artifact smoke-test exception to intentional `sync/repository/` imports. Focused smoke test and lint/format passed; repository-wide preflight remains red on unrelated core/server/CLI/dashboard tests. Report: `task-7-report.md`.
- Task 7 review: FAIL only because the report named the wrong implementation hash; code scope and focused verification passed.
- Task 7 fix round 1 commit `176c2e86d` corrects the report hash and preserves the verification record. Scoped re-review: prior finding addressed; no new Critical/Important/Minor issues.
- Task 7: complete (commits `4bab7e55..176c2e86`, review clean after fix round 1).
- Fresh controller verification: `rtk proxy bun test ./packages/core/__tests__/build-entry.smoke.ts` passed (1/1); `rtk proxy bun run check` and `rtk git diff --check` passed. `rtk proxy bun run preflight` passed lint/types and format, then failed only at `@aio-proxy/cli#test` with 13 existing upgrade-path fixture failures; no core artifact failure remained.
- Task 7 fix round 2 commit `dbac726bd` adds the fresh controller preflight result to the report. Scoped re-review: PASS; no new Critical/Important/Minor issues.
- Task 7 final status: complete after fix rounds 1–2 (commits `4bab7e55..dbac726b`).
- Ledger clarification: the three residual rulings at lines 91–93 were the pre-fix final-review state; the subsequent final-fix and residual scoped review recorded below addressed them in commits `472d58ef` and the prepared-retry follow-up. They remain listed as historical rulings with their original costs.
- Task 8 brief: `task-8-brief.md`; base commit: `3fc68bb189b0bf6d63e761e59482a67771ebe9a6`.
- Task 8 implementation commit `d32935d1e` preserves caller-spelled macOS Homebrew launcher paths, keeps realpath ownership classification, and makes native fixtures derive the current platform/architecture. Focused upgrade tests pass; preflight reaches the server suite but retains five unrelated timing/resource-sensitive failures and one unhandled SQLite/I/O error. Report: `task-8-report.md`.
- Task 8 review: one Important portability gap found because two native-package fixture tests lacked the supported-platform guard.
- Task 8 fix round 1 commit `c720aca5f` adds the `nativeTest` guard to both remaining native-package tests; report commit metadata was corrected in `bd0ae6a7`.
- Task 8 scoped re-review: PASS; both prior findings are addressed and the fix diff has no new Critical/Important/Minor regressions.
- Task 8: complete (commits `d32935d1e..bd0ae6a7`, review clean after fix round 1).
- Final whole-branch review from `ef5f8fb5..bd0ae6a7`: one Important cross-plan contract defect found; CloudKit `modifiedAt` returned epoch seconds while core retention arithmetic requires milliseconds. No Critical findings.
- Final fix commit `f84805215` converts CloudKit modification dates to safe UTC epoch milliseconds, preserves missing/overflow as `outcome-unknown`, and adds native unit/retention regression coverage. Native/plugin checks passed; XCTest execution and live Apple/CloudKit gates remain unavailable and are documented in `final-review-fix-report.md`.
- Final scoped re-review: PASS; the timestamp contract finding is addressed and the fix diff has no new Critical/Important/Minor regressions.
- Final deterministic assessment: ready for merge at the source/test level; signed CloudKit, installed-path/launchd, two-Mac, and provider-specific OAuth gates remain blocked and are not release evidence.
