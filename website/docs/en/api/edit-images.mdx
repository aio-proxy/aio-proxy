---
title: 'Edit images POST /v1/images/edits'
description: 'Edits images from JSON image_url/file_id sources or multipart uploads.'
pageType: doc-wide
outline: false
---

import { ApiOperation } from '../../../src/components/api-operation';

# Edit images `POST /v1/images/edits`

Edits images from JSON image_url/file_id sources or multipart uploads.

<details>
<summary>Parameters / Responses</summary>

**Parameters:** `background`, `image`, `images`, `mask`, `model`, `moderation`, `n`, `output_compression`, `output_format`, `partial_images`, `prompt`, `quality`, `response_format`, `size`, `stream`, `style`, `user`

**Responses:** `200 application/json`, `200 text/event-stream`

</details>

<ApiOperation slug="edit-images" locale="en" />
