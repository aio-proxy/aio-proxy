---
title: 'Create an interaction POST /v1beta/interactions'
description: 'Creates a Gemini interaction.'
pageType: doc-wide
outline: false
---

import { ApiOperation } from '../../../src/components/api-operation';

# Create an interaction `POST /v1beta/interactions`

Creates a Gemini interaction.

<details>
<summary>Parameters / Responses</summary>

**Parameters:** `agent`, `agent_config`, `background`, `environment`, `generation_config`, `input`, `labels`, `model`, `previous_interaction_id`, `response_format`, `safety_settings`, `service_tier`, `store`, `stream`, `system_instruction`, `tools`, `webhook_config`

**Responses:** `200 application/json`, `200 text/event-stream`

</details>

<ApiOperation slug="interactions" locale="en" />
