---
title: 'Create a completion POST /v1/completions'
description: 'Generates legacy text completions.'
pageType: doc-wide
outline: false
---

import { ApiOperation } from '../../../src/components/api-operation';

# Create a completion `POST /v1/completions`

Generates legacy text completions.

<details>
<summary>Parameters / Responses</summary>

**Parameters:** `best_of`, `conversation_id`, `echo`, `frequency_penalty`, `logit_bias`, `logprobs`, `max_tokens`, `metadata`, `model`, `n`, `presence_penalty`, `prompt`, `prompt_cache_key`, `seed`, `session_id`, `stop`, `stream`, `stream_options`, `suffix`, `temperature`, `top_p`, `user`

**Responses:** `200 application/json`, `200 text/event-stream`

</details>

<ApiOperation slug="completions" locale="en" />
