---
title: 'Attach to a realtime call GET /v1/realtime/calls/{call_id}'
description: 'Upgrades to a WebSocket sideband for a caller-owned registered call, retaining its original provider/account.'
pageType: doc-wide
outline: false
---

import { ApiOperation } from '../../../src/components/api-operation';

# Attach to a realtime call `GET /v1/realtime/calls/{call_id}`

Upgrades to a WebSocket sideband for a caller-owned registered call, retaining its original provider/account.

<details>
<summary>Parameters / Responses</summary>

**Parameters:** `call_id`

**Responses:** `101`

</details>

<ApiOperation slug="attach-realtime-call" locale="en" />
