import { Badge } from '@aio-proxy/ui/components/badge';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@aio-proxy/ui/components/card';

# AIO Proxy

<Badge variant="outline">Model gateway</Badge>

<Card className="mt-6">
  <CardHeader>
    <CardTitle>One endpoint, many providers</CardTitle>
    <CardDescription>Route, fail over, and observe model requests without changing client protocols.</CardDescription>
  </CardHeader>
  <CardContent>
    Start with the quick-start guide, then add Providers through the Dashboard or configuration file.
  </CardContent>
</Card>
