---
title: '压缩响应上下文 POST /v1/responses/compact'
description: '通过兼容的原始透传上游压缩响应上下文。'
pageType: doc-wide
outline: false
---

import { ApiOperation } from '../../../src/components/api-operation';

# 压缩响应上下文 `POST /v1/responses/compact`

通过兼容的原始透传上游压缩响应上下文。

<details>
<summary>参数 / 响应</summary>

**参数:** `input`, `instructions`, `model`, `previous_response_id`, `prompt_cache_key`, `prompt_cache_options`, `prompt_cache_retention`, `service_tier`, `stream`

**响应:** `200 application/json`

</details>

<ApiOperation slug="compact-response" locale="zh" />
