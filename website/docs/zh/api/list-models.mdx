---
title: '列出模型'
description: '列出当前可通过 aio-proxy 使用的模型。“Try It” 在浏览器中运行，因此所选服务必须允许跨源请求；浏览器会阻止混合内容，所以 HTTPS 文档页面无法调用 HTTP 服务。'
pageType: doc-wide
outline: false
---

import { ApiOperation } from '../../../src/components/api-operation';

# 列出模型

`GET /v1/models`

列出当前可通过 aio-proxy 使用的模型。“Try It” 在浏览器中运行，因此所选服务必须允许跨源请求；浏览器会阻止混合内容，所以 HTTPS 文档页面无法调用 HTTP 服务。

**参数:** 无

**响应:** `200 application/json`

<ApiOperation slug="list-models" locale="zh" />
