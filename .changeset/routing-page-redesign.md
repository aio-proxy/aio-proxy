---
'aio-proxy': minor
'@aio-proxy/core': minor
'@aio-proxy/server': minor
'@aio-proxy/types': minor
'@aio-proxy/dashboard': minor
---

The routing page groups models by lab and can filter to models with no eligible Provider, only one Provider and no failover, or traffic that diverges from the configured weights. Each model shows the share of traffic it actually served and its success rate beside those configured Provider weights. Editing a route is a full page instead of a drawer.
