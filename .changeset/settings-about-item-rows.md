---
'@aio-proxy/dashboard': patch
'@aio-proxy/ui': patch
'aio-proxy': patch
---

Build the Settings About rows from the shadcn `Item` primitive so the repository and documentation rows are clickable end to end instead of only through their chevron.
