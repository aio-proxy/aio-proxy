---
'@aio-proxy/plugin-sdk': patch
'aio-proxy': patch
---

Refresh dependencies across the workspace, including `eventsource-parser` 4 for SSE parsing, `hono` 4.13.7 for the proxy and Dashboard routes, and `jose` 6.2.12 for token handling. Behavior is unchanged.
