---
'@aio-proxy/plugin-openai-chatgpt': minor
'aio-proxy': minor
---

ChatGPT OAuth providers now expose `gpt-image-2.5-sunburst` and `gpt-image-2.5-flare` on `/v1/images/generations` and `/v1/images/edits`. Blank `model` still defaults to `gpt-image-2`.
