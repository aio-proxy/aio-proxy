---
'@aio-proxy/plugin-sdk': minor
'aio-proxy': minor
---

Plugin Provider fields can declare the protocols they accept. Guardian now asks for a TypeSafe System One Provider, and the model control lists that Provider's routed models instead of accepting any ID.
