---
'@aio-proxy/ui': patch
'aio-proxy': patch
---

Render the dashboard's default-size switches as Safari's native `<input type="checkbox" switch>` when the browser supports it, falling back to the Base UI implementation everywhere else.
