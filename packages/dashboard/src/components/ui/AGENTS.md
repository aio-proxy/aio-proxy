# shadcn UI Components

Files in this directory are managed by the shadcn CLI and must not be modified manually.

To install or overwrite a component, run this command from the repository root:

```sh
bunx --bun shadcn@latest add <component> --overwrite
```

No other installation or modification method is allowed for files in this directory.
