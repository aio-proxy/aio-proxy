/** The condition controls read as one `label + control` chip each, so the addon carries the dimension
 * name in mono and the control gives up its own border to sit inside the group. */
export const CONDITION_LABEL_CLASS = 'w-16 shrink-0 justify-start! font-mono text-xs font-normal';

export const CONDITION_SELECT_CLASS =
  'w-auto! min-w-0 flex-1 rounded-none! border-0! bg-transparent! px-1.5 font-mono text-xs shadow-none focus-visible:border-transparent! focus-visible:ring-0!';
