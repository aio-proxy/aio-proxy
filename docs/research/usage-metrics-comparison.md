# Usage and traffic metric semantics in three AI gateways

Research date: 2026-07-11. Sources are current repository code/schema at these pinned commits:

- `QuantumNous/new-api`: [`ad900bb`](https://github.com/QuantumNous/new-api/tree/ad900bbba74b3e9b16b1ef9c549812ada2bb14a0)
- `Wei-Shaw/sub2api`: [`e316ebf`](https://github.com/Wei-Shaw/sub2api/tree/e316ebf52838a89d57fc790981cce7520f819ac8)
- `ding113/claude-code-hub`: [`595a7d9`](https://github.com/ding113/claude-code-hub/tree/595a7d988a91c730ed63a791b4a92acb5a0e9c41)

## Comparison

| Repository / surface | What one request means; retry/fallback effect | Success / failure semantics | RPM / TPM semantics | Buckets and dimensions |
|---|---|---|---|---|
| **new-api — usage dashboard/log stats** | A row counted by the dashboard is a `LogTypeConsume` row, written after successful settlement. Therefore it is a successful, usage-bearing/billed terminal request, not every inbound request and not every upstream attempt. Failed attempts can create error logs, but are excluded from these request/RPM/TPM queries. A fallback that eventually succeeds contributes one consume row for the final channel. | This surface has no success-rate denominator: it filters to consume rows only. Consequently its request count cannot be used to derive failure rate. | Live RPM is `count(*)`; TPM is `sum(prompt_tokens)+sum(completion_tokens)` over consume rows from the rolling last 60 seconds. Model, channel, group, user and token filters apply. The requested report start/end range does **not** constrain RPM/TPM. The classic overview separately labels averages calculated as selected-range request/token totals divided by selected elapsed minutes. | Exported dashboard data is fixed to one-hour buckets. Stored dimensions include requested/log model, group, token, channel, user and node. Sources: [rolling query](https://github.com/QuantumNous/new-api/blob/ad900bbba74b3e9b16b1ef9c549812ada2bb14a0/model/log.go#L612-L668), [hourly aggregation](https://github.com/QuantumNous/new-api/blob/ad900bbba74b3e9b16b1ef9c549812ada2bb14a0/model/usedata.go#L15-L98), [range-average UI](https://github.com/QuantumNous/new-api/blob/ad900bbba74b3e9b16b1ef9c549812ada2bb14a0/web/classic/src/hooks/dashboard/useDashboardData.js#L108-L120). |
| **new-api — model performance metrics** | One sample is emitted per terminal inbound relay outcome. The retry loop may make several channel attempts, but emits one failure sample only after exhaustion; a later successful settlement emits one success sample. Thus retries do not multiply `request_count`. | `success_count / request_count`; success is the explicit terminal boolean passed by the relay/settlement path, not a SQL HTTP-status test. | No RPM/TPM here. Output tokens are used for average generation TPS, not TPM. | Dimension is origin model + using group. Bucket size is configurable to 1 minute, 5 minutes or 1 hour; default is 1 hour. Sources: [retry loop and terminal failure](https://github.com/QuantumNous/new-api/blob/ad900bbba74b3e9b16b1ef9c549812ada2bb14a0/controller/relay.go#L181-L248), [sample dimensions](https://github.com/QuantumNous/new-api/blob/ad900bbba74b3e9b16b1ef9c549812ada2bb14a0/pkg/perf_metrics/metrics.go#L27-L75), [bucket defaults](https://github.com/QuantumNous/new-api/blob/ad900bbba74b3e9b16b1ef9c549812ada2bb14a0/setting/perf_metrics_setting/config.go#L5-L38). |
| **sub2api — standard dashboard** | `total_requests` is `COUNT(*)` over `usage_logs`. Rows are deduplicated by `(request_id, api_key_id)`, so a recovered fallback remains one usage request rather than one row per attempt. This is principally a successful usage/billing log surface; errors live separately in `ops_error_logs`. | No failure rate is derived from standard `usage_logs` alone. | RPM and TPM are rolling-last-5-minute totals divided by 5 using integer division. TPM includes only input + output tokens, excluding cache token columns. This is a five-minute average, not the current fixed minute and not an instantaneous rate. | Optional standard-dashboard filter is user; broader usage queries group/filter by user, API key, account, model, group/channel and time. Sources: [5-minute calculation](https://github.com/Wei-Shaw/sub2api/blob/e316ebf52838a89d57fc790981cce7520f819ac8/backend/internal/repository/usage_log_repo_dashboard.go#L14-L35), [idempotent insert](https://github.com/Wei-Shaw/sub2api/blob/e316ebf52838a89d57fc790981cce7520f819ac8/backend/internal/repository/usage_log_repo_insert.go#L270-L294). |
| **sub2api — ops traffic/health** | Request traffic is `usage_logs` success rows + client-visible `ops_error_logs` with status `>=400`. Recovered upstream failures may be retained for health investigation, but the client-error count excludes recovered rows by its status predicate. Retry/failover events are counted separately as switches, so they do not inflate request count. | `usage_logs` rows are successes. Errors are final client-visible status `>=400`; `count_tokens` probes are excluded in preaggregation. It also separates business-limited errors, SLA errors, provider errors, 429 and 529. | Realtime data uses fixed one-minute buckets. Peak RPM/TPM are the maximum request/token total in any minute. Average rates are computed across the selected window (the API returns QPS/TPS by dividing totals by seconds); “current” uses the last minute. Separately, throughput trends normalize 1/5/60-minute buckets to QPS/TPS. | Preaggregation uses UTC hourly buckets and grouping sets for overall, platform, and platform+group. Raw trends support 60/300/3600-second buckets. Tokens include input + output + cache creation + cache read. Sources: [hourly definitions](https://github.com/Wei-Shaw/sub2api/blob/e316ebf52838a89d57fc790981cce7520f819ac8/backend/internal/repository/ops_repo_preagg.go#L20-L132), [minute current/peak/average](https://github.com/Wei-Shaw/sub2api/blob/e316ebf52838a89d57fc790981cce7520f819ac8/backend/internal/repository/ops_repo_realtime_traffic.go#L30-L121), [trend and switch counts](https://github.com/Wei-Shaw/sub2api/blob/e316ebf52838a89d57fc790981cce7520f819ac8/backend/internal/repository/ops_repo_trends.go#L15-L105), [error-row scope](https://github.com/Wei-Shaw/sub2api/blob/e316ebf52838a89d57fc790981cce7520f819ac8/backend/internal/repository/ops_repo.go#L965-L982). |
| **claude-code-hub — overview** | One `usage_ledger` row is unique per `message_request.request_id`. Dashboard counts apply a billable/active condition: unblocked rows excluding non-billing endpoints such as warmup, count-tokens and compact. Retries, hedges and fallback attempts are collapsed into the single request; `final_provider_id` and the provider chain preserve routing outcome/details. | Overview error rate is `count(NOT is_success) / count(*)` over billable ledger rows. The trigger defines `is_success` as no error message and status null or `<400`. This is distinct from the more selective provider availability definition below. | Displayed RPM is simply billable ledger rows created in the rolling last 1 minute, optionally by user. It counts completed/finalized ledger rows, not admissions at the rate limiter. No observed dashboard TPM calculation was found; provider `tpm` fields are capacity configuration, not measured traffic. Overview results may be cached for 10 seconds. | “Today” follows configured system-timezone day boundaries. Usage charts use timezone-aligned hourly buckets for today and daily buckets for 7 days, 30 days and current month. Ledger dimensions include user, key, original/current/actual model, original/final provider, endpoint and API type. Sources: [overview query](https://github.com/ding113/claude-code-hub/blob/595a7d988a91c730ed63a791b4a92acb5a0e9c41/src/repository/overview.ts#L43-L192), [billable condition](https://github.com/ding113/claude-code-hub/blob/595a7d988a91c730ed63a791b4a92acb5a0e9c41/src/repository/_shared/ledger-conditions.ts#L15-L35), [ledger uniqueness/dimensions](https://github.com/ding113/claude-code-hub/blob/595a7d988a91c730ed63a791b4a92acb5a0e9c41/src/drizzle/schema.ts#L1040-L1084), [`is_success` trigger](https://github.com/ding113/claude-code-hub/blob/595a7d988a91c730ed63a791b4a92acb5a0e9c41/drizzle/0098_equal_selene.sql#L40-L55), [chart buckets](https://github.com/ding113/claude-code-hub/blob/595a7d988a91c730ed63a791b4a92acb5a0e9c41/src/repository/statistics.ts#L82-L139). |
| **claude-code-hub — provider availability / leaderboard success rate** | Still one finalized client request, not one provider attempt. It uses the final provider-chain outcome; intermediate retries and hedge losers are not separate denominator rows. | Denominator includes only `success_rate_outcome IN ('success','failure')`. Success includes final reasons `request_success`, `retry_success`, `hedge_winner`, or final status 200–399. Blocked/rule-matched requests, 404/499, resource-not-found, concurrency/quota/rate-limit cases, non-retryable client errors, client aborts and hedge losers are excluded. Everything else finalized is failure. | Not an RPM/TPM surface. | Provider availability groups by provider and time bucket; exact dynamic availability bucket selection is separate from overview usage charts. Source: [outcome function](https://github.com/ding113/claude-code-hub/blob/595a7d988a91c730ed63a791b4a92acb5a0e9c41/drizzle/0104_watery_thunderbird.sql#L64-L154) and [countable conditions](https://github.com/ding113/claude-code-hub/blob/595a7d988a91c730ed63a791b4a92acb5a0e9c41/src/repository/_shared/ledger-conditions.ts#L27-L35). |

## Important cross-project differences

1. **“Requests” is not portable.** `new-api`'s usage dashboard and `sub2api`'s standard dashboard primarily count successful usage rows. `sub2api` ops and `claude-code-hub` can represent terminal failures, but in separate or explicitly classified records.
2. **Retries should not inflate client traffic.** All three have a path that collapses fallback/retry into one terminal request. `sub2api` additionally exposes switch events; `claude-code-hub` retains the provider chain. `new-api` error logs may be emitted per failed channel attempt, so those raw error-log rows must not be mistaken for inbound request count.
3. **“Success rate” has policy choices.** `sub2api` ops treats client-visible `>=400` as errors, with separate SLA/business/provider subsets. `claude-code-hub` overview uses a broad terminal `is_success`, while provider availability deliberately excludes client, quota, policy and hedge-loser outcomes. `new-api`'s model performance rate uses an explicit terminal relay boolean.
4. **RPM labels conceal different windows.** `new-api` and `claude-code-hub` use a rolling 60-second count; `sub2api` standard dashboard reports a rolling five-minute average with integer truncation. Range-average RPM in `new-api` is yet another metric.
5. **TPM token sets differ.** `new-api` and `sub2api` standard TPM exclude cache-token categories; `sub2api` ops traffic includes them. `claude-code-hub` has no observed traffic TPM on the overview.

## Recommendation for aio-proxy's overview

Adopt one terminal request record as the stable denominator instead of deriving traffic from usage-bearing success logs:

- **Requests and retries:** one routed inbound request produces one terminal `request_log` row with a `success`, `failure`, or `cancelled` outcome. Fallback attempts do not create additional traffic rows; their provider, model, protocol, outcome, and order are retained only as ordered metadata on that request log.
- **Usage attribution:** only the final successful attempt may produce a usage row keyed by request id. Token and estimated-cost metrics use that final attempt's reported input and output tokens; failed attempts contribute neither tokens nor cost.
- **Overview cards:** show all terminal requests, `success / (success + failure)`, known estimated cost with pricing coverage, input plus output tokens, Average RPM, and Average TPM. Cancelled requests remain visible but do not enter the success-rate denominator.
- **Average rates:** calculate Average RPM as terminal requests divided by the selected range's actual elapsed minutes, and Average TPM as final-success input plus output tokens divided by those same elapsed minutes. Rolling 60-second counters used by some compared projects are not adopted for this overview.
- **Buckets and dimensions:** use hourly buckets for 24h and server-local calendar-day buckets for 7d, 14d, and 30d. Group the stacked chart by the final model or provider. Separate per-attempt counters and attempt-level chart series, although present in some compared systems, are not part of the current design.

This keeps retries diagnosable without inflating client traffic or attributing failed-attempt usage to the successful routed request.
